# Fingerprint
set_prop(init, vendor_fingerprint_prop)

# Vibrator (aw8697/RichTap) - init touches the node before hal_vibrator_default takes over
allow init aac_richtap_dev_device:chr_file { open read write ioctl };
